extends base.pug

block content
  ul.article-list
    each article in articles
      li.article-item
        a(href=article.route)= article.title
        time(datetime=article.date)= article.formattedDate
