extends base.pug

block content
  ul.article-list
    each article in articles
      li.article-item
        a.article-title(href=article.route)= article.title
        time.article-date(datetime=article.date)= article.formattedDate
