extends base.pug

block content
  article.article
    header
      h1.title= article.title
      time(datetime=article.date)= article.formattedDate
    each line in article.content.split('\n')
      p= line
